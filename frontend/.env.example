# Dev default: Vite proxies /api → http://127.0.0.1:8000 (see vite.config.ts)
VITE_API_BASE_URL=/api

# Set to "true" to skip the backend and always use the in-browser demo pipeline.
# VITE_USE_MOCK=true
